define(function() {

	return Backbone.View.extend({
		initialize: function(options) {
		},
		
		render: function() {
		    _private.headerView = new HeaderView();
		    _private.headerView.render();
		    
			$("<div>").text("Main content").appendTo(document.body);
			return this;
		}
	});

});
